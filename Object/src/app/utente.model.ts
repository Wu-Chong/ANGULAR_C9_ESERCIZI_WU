export class Utente{ // creo un classe utente con i suoi relativi attributi
  constructor(public nome: string /*per dichiarare una variabile si mette ':'*/, public cognome: string, public username: string, public email: string, public password: string){
  }
}
